---
'foldkit': patch
'@foldkit/devtools': patch
---

Slim DevTools outline drawing to imperative painters with Effect only at resource boundaries, route outline enable through a Command, and add `foldkit/outline` for shared outline event types and recording helpers.
