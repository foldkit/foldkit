---
'@foldkit/ui': minor
---

Add `Meter` and `Progress` stateless controlled views. `Meter` renders a scalar value in a known range with `role="meter"`, clamped `aria-valuenow`, and threshold data attributes. `Progress` renders task progress with `role="progressbar"`, with determinate state for numeric values and indeterminate state that omits the value attributes.
