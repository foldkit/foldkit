---
'@foldkit/devtools': minor
---

The Commands tab now shows each Command's destination Submodel path. When a Command carries a `submodelPath`, the overlay renders the wrapper tags from outer to inner as a small label above the Command, so you can see which Submodel a Command was attributed to without inspecting the Message that will eventually arrive.
