---
'foldkit': patch
---

Fix devtools overscroll behavior: add overscroll-none CSS utility and apply it to the inspector tree and message list panes so scrolling within DevTools panels doesn't propagate to the page.
