---
'@foldkit/ui': minor
---

Add `Ui.Nav`, a stateless primitive for URL-driven navigation between routes. It renders a navigation landmark whose items are links and marks the current destination with `aria-current="page"`, the semantics the ARIA Authoring Practices Guide recommends for moving between pages. Reach for `Ui.Tabs` instead when switching content within a single page.
