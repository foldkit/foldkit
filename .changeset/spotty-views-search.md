---
'foldkit': minor
---

Add an experimental `DataCommand` module implementing the Elm-style single `execute` Command pattern (Commands as inert tagged data interpreted at the runtime boundary). Spike only, built to evaluate the pattern against the existing Command model. See `experiment/execute-pattern/FINDINGS.md`.
