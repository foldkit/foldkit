---
---

Spike a private `@foldkit/prettier-plugin-view` package exploring an
elm-format-inspired layout for ECMAScript array, object, and call
expressions. Not published; no source files outside the new package
are modified. See `notes.md` and the package's `artifacts/` directory.
