{
  "name": "{{name}}",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node scripts/serve.mjs",
    "typecheck": "tsc --noEmit",
    "format": "prettier -w .",
    "test": "vitest run",
    "lint": "oxlint src"
  }
}
